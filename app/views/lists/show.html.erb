<h1><%= @list.name %></h1>
<% if not @list.items.empty? %>
    <h2>Items</h2>
    <% @list.items.each do |item| %>
    <h3><%= item.name %></h3>
    <% end %>
    <% end %>
<%= link_to "Add Item", new_item_path(@list) %>
<%= link_to "Back to lists", lists_view_path %>
